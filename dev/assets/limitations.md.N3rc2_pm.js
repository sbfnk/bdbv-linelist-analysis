import{c as a,S as t,j as i,m as o}from"./chunks/framework.CJeXZ5eh.js";const m=JSON.parse('{"title":"Limitations","description":"","frontmatter":{},"headers":[],"relativePath":"limitations.md","filePath":"limitations.md","lastUpdated":null}'),r={name:"limitations.md"};function s(n,e,l,d,h,c){return t(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h1><h1 id="data" tabindex="-1">Data <a class="header-anchor" href="#data" aria-label="Permalink to &quot;Data&quot;">​</a></h1><ul><li><p>n = 52 cases is small. Posteriors are tight for the well-anchored delays (onset → admit n = 40; onset → death via convolution n ≥ 22) and wide for sparse or heavy-tailed ones (admit → discharge n = 15; onset → notification has max observed 86 d).</p></li><li><p>No transmission pairs in the deposit. Serial interval, generation interval and R₀ cannot be estimated. The Rosello deposit is canonical for Isiro and the underlying source data is not available.</p></li><li><p>No exposure dates. Incubation period cannot be fitted. Use MacNeil 2010 (mean 6.3 d, 95% CI 5.2–7.3, n = 24, 2007 Uganda) instead.</p></li><li><p>5 admission-date encoding outliers (−89, −5, −4, −1, 328720 days from onset) and 1 notification-date outlier (−62 days) are set to missing during loading. These look like onset/secondary date swaps but we cannot verify the direction with the custodian, so they are dropped rather than swapped.</p></li><li><p>3 of 52 cases have missing age, imputed at the sample mean before standardisation.</p></li><li><p>Notification has a long right tail (47, 50, 53, 60, 65, 75, 86 days after onset) — likely retrospective notifications of community deaths investigated post hoc. Gamma handles this better than LogNormal; the mean (≈ 20 d) is still well above the median (≈ 11 d).</p></li></ul><h1 id="model" tabindex="-1">Model <a class="header-anchor" href="#model" aria-label="Permalink to &quot;Model&quot;">​</a></h1><ul><li><p>The HCW-stratified delay model (<code>bdbv_model_stratified</code>) still uses the marginalised <code>double_interval_censored</code> formulation and so treats per-case censoring as independent across delays. The main <code>bdbv_model</code> was converted to per-case latents (see &quot;Model&quot; in <code>MODEL.md</code>); the stratified variant is a smaller analysis used only for the HCW-vs-non-HCW odds-ratio table and has not yet been ported.</p></li><li><p>No latent severity links delays and mortality. The delay fits and the CFR fits do not share information about case severity.</p></li><li><p>Age effect is linear in standardised age. Nonlinearities (e.g. U-shape) are not captured.</p></li><li><p>Sex is not in the CFR model. Rosello records sex (40 F / 12 M in Isiro 2012) but Kratz 2015&#39;s community-vs-ETC stratification is not reproducible from the deposit.</p></li><li><p>5 community deaths (died without recorded admission) contribute to the CFR block but not to the delay block; the model has no pathway for non-admitted deaths.</p></li><li><p>The stratified delay model is restricted to the Gamma family. The LogNormal parametrisation in the stratified model would need a different SD prior to be consistent with the unstratified fit, and the Weibull shape prior needed extra truncation that complicated the stratified parametrisation. Gamma is the WAIC winner anyway.</p></li></ul><h1 id="inference" tabindex="-1">Inference <a class="header-anchor" href="#inference" aria-label="Permalink to &quot;Inference&quot;">​</a></h1><ul><li><p>InitFromPrior works here. The bounded-primary reparametrisation on the per-case latents means NUTS sees a smoothly-bounded support (no hard <code>t_s &gt; t_p</code> constraint), so warm-up is well-behaved.</p></li><li><p>Single seed (20260519). The convergence diagnostics are strong but a multi-seed run is a useful robustness check.</p></li></ul><h1 id="generalisability" tabindex="-1">Generalisability <a class="header-anchor" href="#generalisability" aria-label="Permalink to &quot;Generalisability&quot;">​</a></h1><p>BDBV is the third <em>Ebolavirus</em> species, with ~250 cases globally across three outbreaks (Uganda 2007–08, DRC 2012, DRC/Uganda 2026). Isiro is the only delay parametrisation we have. The Isiro outbreak was rural, mining-influenced, with MSF ETC support. The 2026 Ituri outbreak has urban (Bunia) and conflict-zone components that may push delays longer.</p><h1 id="out-of-scope" tabindex="-1">Out of scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to &quot;Out of scope&quot;">​</a></h1><ul><li><p>Whether the HCW protective effect is causal (faster access to care) or selection (HCWs were preferentially confirmed). The probable-vs-confirmed adjustment partially controls for the latter but does not eliminate it.</p></li><li><p>Transmission. Use Zaire-EVD generation interval priors for downstream Rt or nowcasting work.</p></li><li><p>Reservoir or spillover dynamics.</p></li></ul><h1 id="downstream-priors" tabindex="-1">Downstream priors <a class="header-anchor" href="#downstream-priors" aria-label="Permalink to &quot;Downstream priors&quot;">​</a></h1><p>For downstream re-applications anchored on the 2012 Isiro outbreak (e.g. <code>epiforecasts/BVDOutbreakSize</code>), the exploratory exponential fit in <a href="./analysis#Early-phase-growth-rate">Analysis walkthrough § Early-phase growth rate</a> is the recommended source of a growth-rate prior. As a one-line summary, use <strong><code>Normal(0.008, 0.005)</code> on <code>r</code> per day</strong> (posterior mean and SD from a Poisson regression on weekly onset counts from week 1 through the peak week). The 95% CrI on <code>r</code> covers zero — Isiro was a slow, noisy rise — so treat this as a weakly-informative prior rather than a tight constraint, and re-run <code>fit_growth_rate(ll)</code> if you want the live posterior summary.</p>',13)])])}const u=a(r,[["render",s]]);export{m as __pageData,u as default};
